# =============================================================================
# Project-specific, exact-pinned lock for the fNIRS kappa-correction package.
# =============================================================================
#
# This lock contains ONLY the project's declared dependencies (requirements.txt)
# and their full transitive closure -- NOT a broad system-wide `pip freeze`. It
# is therefore portable: installing it into a clean interpreter reproduces the
# exact environment used to generate the frozen release artifacts, without
# dragging in unrelated packages that happened to be present on the build host.
#
# Python:       3.11.15
# Platform:     Linux-6.18.5-fc-v18-x86_64-with-glibc2.39 (x86_64)
# Closure:      38 packages (project + transitive), resolved from the frozen
#               build environment.
#
# How this was generated (dependency closure of the direct requirements,
# pinned to the versions actually used):
#   pip install pipdeptree
#   # walk the dependency graph from the roots in requirements.txt
#   # (numpy scipy matplotlib pandas mne mne-nirs mne-bids h5py) and emit
#   # each reached package at its installed version.
#
# Equivalent, tool-standard regeneration with pip-tools (recommended for users
# who want hashes and a self-updating workflow):
#   pip install pip-tools
#   pip-compile --generate-hashes --output-file requirements-lock.txt requirements.txt
# or with uv:
#   uv pip compile requirements.txt -o requirements-lock.txt
#
# Install the exact frozen environment:
#   pip install -r requirements-lock.txt
#
# Note: `jupyter` (declared in requirements.txt for the OPTIONAL supplementary
# notebook) is NOT part of this results-generating closure and is intentionally
# excluded here; install it separately if you want to run the notebook:
#   pip install jupyter
# -----------------------------------------------------------------------------

# --- direct project dependencies (declared in requirements.txt) ---
numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.9
pandas==3.0.2
mne==1.12.1
mne-nirs==0.7.3
mne-bids==0.19.0
h5py==3.16.0

# --- transitive dependencies (pulled in by the above) ---
certifi==2026.4.22
charset-normalizer==3.4.7
contourpy==1.3.3
cycler==0.12.1
decorator==5.3.1
fonttools==4.62.1
h5io==0.2.5
idna==3.13
importlib-resources==7.1.0
jinja2==3.1.6
joblib==1.5.3
kiwisolver==1.5.0
lazy-loader==0.5
markupsafe==3.0.3
nibabel==5.4.2
nilearn==0.14.0
packaging==26.1
pillow==12.2.0
platformdirs==4.9.6
pooch==1.9.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
requests==2.33.1
scikit-learn==1.8.0
seaborn==0.13.2
six==1.17.0
threadpoolctl==3.6.0
tqdm==4.70.0
typing-extensions==4.15.0
urllib3==2.6.3
